"""unfold — WaterGAP UNF → GIS tools.

Modules:
  grid       shared georeference + UNF/grid readers
  convert    UNF → GeoTIFF / GeoPackage (CLI + GUI)  → command `unfold`
  view       view UNF (stats, map, PNG/CSV)          → command `unf-view`
"""

__version__ = "1.0.0"
